# Silas Bot 🖤

A Discord bot powered by Anthropic's Claude API. He's got personality. He's got opinions. He's got a 🖤.

## Setup

1. Set environment variables (see below)
2. Deploy to Railway connected to this repo

## Environment Variables

- `DISCORD_TOKEN` — Your Discord bot token
- `ANTHROPIC_API_KEY` — Your Anthropic API key
- `ANNIE_USER_ID` — Annie's Discord user ID
- `ALLOWED_CHANNELS` — Comma-separated channel IDs where Silas responds
- `CONTEXT_LENGTH` — Number of recent messages to remember (default: 10)
